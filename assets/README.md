# Icon Source

Place a square PNG at `assets/app-icon.png` to override the generated default release icon.

The build converts that PNG into `build/app-icon.ico` for the Windows executable and also reuses the generated PNG for the Linux AppImage icon.
